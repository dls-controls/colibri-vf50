# Access to u-boot environment.

ifndef FW_PRINTENV
$(error Must define FW_PRINTENV)
endif

option:
	$(install) $(FW_PRINTENV) /sbin/
	$(install) $(optiondir)/fw_env.config /etc
	ln -s fw_printenv $(sysroot)/sbin/fw_setenv

# vim: set filetype=make:
